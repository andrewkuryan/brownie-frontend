@import "@colors.styl"

.textAreaField
  position relative
  box-sizing border-box
  border-radius 4px
  border 1px solid lightGray
  font-size 18px
  transition box-shadow 0.3s ease-in-out
  resize none
  &:focus
    outline none
    box-shadow 0 0 4px rgba(primeGreen, 0.8)