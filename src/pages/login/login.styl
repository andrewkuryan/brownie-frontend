.loginRoot
  width 100%
  height 100%
  display flex
  align-items center
  justify-content center
  .loginCard
    background-color white
    width 60%
    height 80%
    box-shadow 0 0 8px rgba(0, 0, 0, 0.2)
    display flex
    flex-direction column
    align-items center
